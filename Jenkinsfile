pipeline {
    agent {
        node {
            label 'desktop-docker-jenkins-dotnet2'
        }
    }
    stages {
        stage('Test') {
            steps {
                sh '. /etc/profile; dotnet --version'
                
                
            }
        }
    }
}